# AK_Exusiai 当前进度

最后更新：2026-09-01
工作分支：`mechanic/v1-relic-logistics`

## V1 大修协调

- 当前设计源为 `AK_Exusiai-Card_V1.csv` 与 `AK_Exusiai-Relic_Potion_V1.csv`；旧版已移入 `docs/archive/`。
- 大修前 `main` 固定在 `40da91d`，以注释标签 `v0.1-pre-overhaul` 标记。
- 第一阶段从设计冻结提交拆分：`mechanic/v1-ammo` 负责弹药、过载和弹药 UI；`mechanic/v1-relic-logistics` 负责快递、中转、遗物选择、失效过滤与跨战斗状态。
- 弹药协作者的完整起始说明位于 `docs/V1_AMMO_CODEX_PROMPT.md`；两条机制分支完成后回到 `mechanic/v1-overhaul` 收口共享文件。

## 当前结果

- 新增统一的 `exusiai` 战斗测试控制台入口：`logic` 可用 100 能量和 10 张新遗物快递测试牌替换手牌，`logic transit` 可换成其余 6 张中转测试牌，两组均支持升级版且不超过手牌上限；执行前通过原版整手弃牌流程把当前手牌放入弃牌堆，正常处理动画与弃牌 Hook，不会删除原手牌或遗留无法打出的幽灵牌。`hand` 显示从 0 开始的有效手牌索引，`replay`、`ammo` 可分别添加重放和设置弹药；旧卡牌快递测试命令已移除。详细用法见 `docs/EXUSIAI_TEST_COMMANDS.md`。
- V1 遗物物流核心已落地：任意遗物可持久化快递/中转状态；失效同时过滤局内与战斗 Hook；快递使用红色滤镜与左上红色层数，中转使用蓝色滤镜与右上蓝色层数，双状态混合为紫色；中转副本绕开原版遗物袋，0 层保留并可重新点亮。
- 遗物栏仅重排显示节点，不改变玩家遗物或 Hook 顺序；按头环置后、永久优先、快递升序、中转降序、获得时间升序稳定排列。物流角标使用原生计数层级，会随选择遮罩一同变暗。
- 遗物栏末尾会在存在中转遗物或头环时显示原版金色箭头按钮：默认展开并朝左，收起后朝右、隐藏全部中转遗物和头环，并以角标显示隐藏数量；按钮使用原生遗物的 68×68 布局格与 44×44 可见箭头，直接读取游戏箭头资源，不会实例化遗物详情层。折叠状态跟随本局玩家跨房间保留，隐藏仅影响显示和焦点链，不影响遗物效果或选择逻辑。
- 原版单行遗物选择框在 9 件以上改为每行 8 件、最多 3 行；超过 24 件时可用滚轮逐行浏览，并同步维护鼠标、键盘与手柄焦点。
- V1 中转池按共享＋能天使普通/罕见/稀有构建，允许永久同名遗物并存，排除已中转/失效中转类型；随机顺序稳定并使用局内 RNG，遗物选择使用多人同步的原版选择界面。
- 已迁移首批物流牌并新增企鹅中转站、货如轮转；Boss's Medal 已改为稀有、每回合首次成功给予遗物快递时获得 10 格挡。
- 能天使角色、77 最大生命、99 初始金币、弹药资源、初始牌组和初始遗物已实现。
- 20 张普通牌及德克萨斯、圣城净化已完成游戏内回归。
- 25 张稀有牌、35 张罕见牌及其余物流/圣城衍生牌已完成代码首版。
- 初始遗物加 8 个新增遗物、3 个角色药水已完成代码首版与中英文本；遗物/药水主图和白色轮廓图由仓库脚本从已跟踪源图生成。
- 首轮反馈已修复：天使最低费用、童年费用联动、缓慢逐牌叠加、干扰持续回合、双发预览、火力掩护逐次减力、卡牌消耗与关键词/描述问题。
- 已同步最新卡表：双发套件改为普通、准备万全！改为罕见；武装护送更新中文名；Move Out!、Hot Pursuit、What Lies Ahead?、Swear on This Gun、Confession 已同步类名、公开 ID 和本地化键。
- 角色选择小图使用官方半身图，背景使用 4K CG；能力图标已重新导入并进入 PCK。
- 生成特定衍生牌的卡牌已补齐卡牌详情；随机生成多个衍生牌的卡牌固定展示目录前三张。
- 多人状态差异包已定位到天使费用查询写状态：现改为仅在确定性费用修改后记录战斗内最低费用；补齐结束回合 ping，并修正“圣徒行记”角色类型注册冲突。
- 双发、福音摇滚与守护铳使用 2026-08-24 更新图标；守护铳切换为 `守护铳_剪影.svg`。
- 卡图测试接入进行中：正式资源目录已有 73 张 PNG；未覆盖卡牌继续使用框架占位图。
- 卡牌基类现按 C# 类名自动绑定 `images/cards/<ClassName>.png`，缺图时回退占位图；四张直接继承 `ModCardTemplate` 的初始牌已统一接入该基类，打击/防御不再强制使用铁甲战士肖像。
- 快递选牌提示现统一显示即将添加的快递层数；守护铳图标显示距离下次抽牌还需消耗的弹药数；失乐园补齐混乱说明；天使的祝福+按最新卡表改为耗能 1。
- 拥抱新生+会显示 `X+1`；回望来路改为必选一张并在选择后直接返回手牌；优秀毕业生移除多余的“仍执行当前意图”文字。
- 包裹已同步最新设计：初始快递 3，层数每变化 1 获得 4（升级后 6）格挡，不再抽牌。
- 弹药说明已补充多发弹药逐发增伤；火力相关悬浮说明会传入实际层数，能力说明中的数值统一标蓝，临时火力嵌套说明继续使用无虚假数值的通用文本。
- 角色头像运行时资源改为原版一致的 85×85，避免历史记录界面按 339×339 源图撑大布局；高分辨率源图保持不变。
- 告解仿照原版黑暗之拥延后处理回合结束的虚无消耗抽牌，使抽到的牌保留到下回合；企鹅大件按最新卡表改为 2 费、升级后 1 费。
- 行动开始！的具名中转遗物已由坚固钳子修正为船夹板；愿主同在按最新卡表改为 1 费，升级仍只增加中转层数。
- 已同步 `V0.1` 平衡版卡表：20 项机制、数值或文本调整完成；穿透打击按“万向斩”传递主目标实际伤害，福音摇滚改为提高攻击牌弹药消耗上限，激光炮改为放大单发弹药的完整伤害加成。简明改动说明位于 `docs/archive/V0.1_BALANCE_CHANGES.md`。
- 编译期依赖、Mod 最低运行时依赖和本机运行时已同步升级到 RitsuLib 0.5.18；游戏 API 基线保持 0.111.0。
- 项目内 Godot 卡图管理器已统一为背景、素材、占位图形三层合成：背景设置采用紧凑三列布局，并支持低倍率、旋转翻转、三态标记、文件元数据、受限缓存与确认清理、自动收起的批量导出进度，以及直接打开卡图输出目录；旧清单会自动迁移至版本 4。
- 已下载能天使 4 套、新约能天使 2 套外观的全部“正面/基建” Spine 源素材；能天使默认战斗与基建骨骼已从 Spine 3.8 转换为 4.2.43。默认战斗模型已替换静态站姿并接入 `Start/Idle/Attack/Die`，缩放调整为 0.75；商店与火堆场景分别接入基建模型的 `Relax` 与 `Sit`，并补齐战斗/结算界面对大小写敏感的 `Die` 兼容处理。PRTS 基建 PNG 的 312×312 下载尺寸现已规范化为 atlas 声明的 468×468，修复部件错采样和散落显示。
- 多人模式的指向、石头、布、剪刀四张手部贴图已接入角色多人资源，并已单独提交到 `main`。
- 六套外观已全部转换并接入：角色选择界面左上角横向提供“角色/时装”两级选择，分别记忆能天使和新约能天使的服装；背景场景沿用妮芙 Mod 的固定画布补偿布局，以抵消游戏 `AnimatedBg` 自带的偏移和 1.1 倍缩放。两组选项均为 240 px 宽，英文长名称自动换行；`Idle` Spine 预览位于角色选项正下方，四个箭头固定在各自名称框内部两端。战斗、商店、火堆会读取相同配置；新约攻击直接播放一次 `Attack_Loop`，施法使用 `Skill_3_Skill`；新约战斗骨骼只保留运行时所需动画并在选择后后台预载，攻击与施法切入前都会清除旧轨道并恢复 setup pose，同时剔除对应动画的空变换时间轴，保留用于隐藏插槽附件的空 attachment 关键帧，避免寻翼之歌阻塞卡牌飞行动画或显示无关部件。非默认时装在商店循环 `Special`，商店交互统一播放一次 `Interact`，不再追加无限循环队列；火堆统一播放 `Sit`。

## 最近验证

- `exusiai` 控制台测试入口及其快递/中转分组修正完成快速与完整构建，0 警告、0 错误，PCK 导出和部署成功；部署 DLL 与构建 DLL 的 SHA-256 一致。Steam 启动完成主菜单审计，26/26 个玩法补丁与 166/166 项自动注册成功，公开的 `ExusiaiConsoleCmd` 类型可从部署产物正常反编译，日志无控制台命令扫描或类型加载异常。`logic` 现使用带正式玩家选择上下文的 `CardCmd.Discard` 整手弃牌，并保留执行前测试分组上限保护。
- V1 遗物物流核心与首批物流牌快速、完整构建均通过：0 警告、0 错误；中英本地化 JSON 全量解析通过，`git diff --check` 无空白错误。
- 完整 `dotnet build .\AK_Exusiai.csproj` 已成功导出并部署 PCK；项目 DLL 与部署 DLL SHA-256 一致。
- Steam 环境真实启动到主菜单并稳定运行 60 秒：AK_Exusiai 的 26/26 个玩法补丁、RitsuLib 的动态补丁和 166/166 项自动注册均成功；物流角标、遗物栏排序、折叠按钮及多行选择框补丁均正确安装，日志无 Mod 初始化错误或异常。
- 折叠按钮缩放与遗物详情层修复后，快速及完整构建、PCK 导出和部署均成功，0 警告、0 错误；Steam 启动 60 秒，26/26 个玩法补丁与 166/166 项自动注册成功。
- PCK 导出成功；9 个遗物和 3 个药水的主图/轮廓图、中英文 `relics.json`/`potions.json` 均出现在打包记录中，制作脚本本身已从 PCK 排除。
- Steam 环境真实启动冒烟测试：RitsuLib 与 AK_Exusiai 初始化成功，153/153 自动注册成功，9 个遗物、3 个药水公开 ID 正确，`ModelDbDefer` 完成，未出现 Mod 初始化或规范模型错误。
- 遗物轮廓图为 256×256，药水主图和轮廓图为 80×80；本轮更新的角色头像和初始证章已进入运行时资源。
- 本轮 C# 快速构建：0 警告、0 错误。两份多人状态差异报告都只在天使衍生牌的 `lowestCombatCost`（`1` 与未初始化）上不同，其余牌堆、生命、能力、随机数与 Mod 版本一致。
- 卡图管理器 Godot 冒烟测试通过：识别 99 个卡牌类、35 张攻击、44 张技能、20 张能力和 2 张先古卡；低倍率插画、透明 SVG 图标、默认普通 500×380 与先古 500×702 占位图均能正确生成；工具新增后 C# 快速构建仍为 0 警告、0 错误。
- 卡图管理器界面增强冒烟测试通过：五档整体缩放、双分隔栏、素材文件夹筛选和动态紧凑网格均成功初始化；用户界面偏好与受限缩略图缓存保存在 `user://`。
- 卡图管理器统一合成冒烟测试通过：500×380 低倍率插画、透明 SVG、500×702 先古占位、旋转翻转、纯色无纹理和中心放射纹理均成功输出；紧凑背景三列的选项数量与清单版本 4 均验证通过。
- 卡图管理器性能更新冒烟测试通过：素材签名缓存完成初始化，刷新时复用未变化图片；批量导出进度控件会在逐张 PNG 编码之间让出主线程，并在完成后复位隐藏；缓存清理确认框和输出目录解析成功初始化。
- 卡图管理器缓存与标记验证通过：磁盘缓存未超过 128 MB 上限，右栏统计与三态 `○/◇/●` 标记均成功初始化；顶部清理操作仅以 `user://card_art_manager_thumbnails/` 中的 PNG 为目标。
- 朋友端日志未出现百科界面的 AK 异常，但发现“圣徒行记”先古注册把同名卡牌误作角色，以及云存档 `modded/profile1/...backup` 删除失败；前者已修复，后者属于游戏/文件系统环境，需朋友端复测。
- 快递提示、守护铳计数、失乐园提示与天使的祝福升级修改后，C# 快速构建及中英本地化 JSON 解析均通过，0 警告、0 错误。
- 本轮拥抱新生、回望来路、包裹、弹药/火力说明和角色头像修正后，完整构建、PCK 导出与部署成功，0 警告、0 错误；14 份中英本地化 JSON 解析通过，运行时角色头像确认为 85×85，部署 DLL 与构建 DLL 的 SHA-256 一致。
- 告解回合末虚无抽牌与企鹅大件 2→1 升级修改后，快速及完整构建均为 0 警告、0 错误，PCK 导出与部署成功。
- `V0.1` 平衡修改完成后，C# 快速构建、完整构建、PCK 导出与部署成功；14 份中英本地化 JSON 解析通过，0 警告、0 错误。
- RitsuLib 0.5.17 升级后完整构建、PCK 导出与部署成功，0 警告、0 错误；Steam 环境启动到主菜单，RitsuLib 0.5.17/游戏 API 0.111.0 正确加载，框架补丁全部成功，AK 自动注册 159/159，`ModelDbDefer` 完成，日志无实际错误或异常。
- 普通 Godot 4.5.1 Mono 加载项目内 SpineGodotExtension4.5.1 成功；Spine 类型探针、4.2.43 转换结果回读、默认角色场景实例化、完整构建、PCK 导出和独立 PCK 场景实例化均通过，0 个 C# 警告/错误。PCK 已排除本机 Spine 编辑器扩展，并关闭文本资源二进制转换以避免自定义 Spine 资源依赖失效。
- 死亡兼容、角色缩放、商店 `Relax` 与火堆 `Sit` 接入后，快速及完整构建均为 0 警告、0 错误，PCK 导出与部署成功；构建 DLL 与部署 DLL 的 SHA-256 一致。普通 Godot、独立测试 PCK 及游戏目录中的正式 PCK 均成功加载战斗、商店和火堆三个 Spine 场景，并确认 `Idle/Attack/Die/Relax/Sit` 动画全部存在。
- 基建纹理尺寸修正并按妮芙实现补入显式场景补丁：商店在 `_Ready/PlayAnimation` 强制映射并循环 `Relax`，火堆在 `_Ready` 后循环 `Sit`。快速及完整构建均为 0 警告、0 错误，DLL/PCK 已部署；正式 PCK 的第 2/60 帧哈希不同，确认 `Relax` 与 `Sit` 时间轴均在推进。原始下载 PNG 保持不变，运行时副本为 468×468。
- 外观切换分支快速及完整构建均为 0 警告、0 错误，DLL/PCK 已部署。项目与正式 PCK 中的 12 份 Spine 骨骼资源、两张选择背景和中英本地化均已加载验证；六套战斗/基建资源所需的 `Idle/Attack/Die`、新约三段攻击与 `Skill_3_Skill`、`Relax/Special/Sit/Interact` 动画名全部存在。
- 角色选择 UI 已恢复妮芙 Mod 使用的固定 `2560×1200` 根画布和 `1920×1078` 背景补偿区域，避免自定义场景再被游戏 `AnimatedBg` 放大、偏移；背景顶部偏移保持为 0、仅向底部延伸 16 px，以补足 16:10 下的底边。两组选项压缩为相邻的 240 px 宽区域并支持英文换行；箭头恢复妮芙 Mod 的 `.205/.295` 横向比例点与 `-165/-20` 模板偏移。放弃游戏结算补丁已从失效的旧默认骨骼路径改为匹配六套战斗骨骼，并统一请求大小写正确的 `Die`。

## 下一步

1. 游戏内依次执行 `exusiai logic`、`hand`、`replay 0 10`、`ammo 22`，目视确认原手牌进入弃牌堆、生成 10 张新遗物快递测试牌、索引有效、重放次数和弹药 UI 正确；再用 `logic transit upgraded` 检查 6 张升级版中转测试牌，并确认两次换手都没有幽灵牌或手牌溢出。
2. 游戏内回归快递 1/2/99 的本场计数、提前恢复、跨战斗存档；确认开战效果不回滚也不重放，快递与 T0 任一失效原因都不会接收 Hook。
3. 回归中转同名永久副本、具名 T0 复用加层、随机中转排除规则、企鹅标快连续两个随机结果、企鹅跨境 T0 重新点亮与原版遗物袋独立性。
4. 游戏内目视检查物流角标会随选择遮罩一同变暗；验证头环/永久/中转/快递层数排序，以及折叠按钮的方向、隐藏数量、跨房间状态、新增/移除遗物刷新和鼠标/手柄焦点。继续验证 9/24/25/50 件遗物选择时的换行与滚轮。
5. 朋友端使用新 DLL/PCK 重新打开百科；若仍失效，记录具体入口、界面表现和新日志，并先处理其 `modded/profile1` 云存档备份删除失败问题。
6. 合并弹药分支前，先在 `mechanic/v1-overhaul` 统一处理 `Entry.cs`、共享本地化与旧卡牌移除，再做完整 PCK/部署和多人回归。

完成一批工作后只更新本页的“当前结果、最近验证、下一步”，不要追加历史流水账。
